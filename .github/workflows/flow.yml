name: Build and Push Docker Image

on:
    push:
        paths:
            - 'public/**'
            - 'admin/**'
            - 'config/**'
            - 'sql/**'
            - 'Dockerfile'
            - 'k8s/**'
            - '.github/workflows/**'
        branches:
            - main # trigger when you push to main branch

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            # 1. Get your code
            - name: Checkout code
              uses: actions/checkout@v4

            # 2. Login to Docker Hub
            - name: Login to Docker Hub
              uses: docker/login-action@v3
              with:
                  username: ${{ secrets.DOCKER_USERNAME }}
                  password: ${{ secrets.DOCKER_PASSWORD }}

            # 3. Build Docker image
            - name: Build image
              run: |
                  docker build -t ${{ secrets.DOCKER_USERNAME }}/PortfolioCMS:latest \
                  -t ${{ secrets.DOCKER_USERNAME }}/PortfolioCMS:${{ github.sha }} .

            # 4. Push to Docker Hub
            - name: Push image
              run: docker push ${{ secrets.DOCKER_USERNAME }}/PortfolioCMS:latest

            # - name: Deploy to Kubernetes
            #   run: |
            #    kubectl apply -f k8s/